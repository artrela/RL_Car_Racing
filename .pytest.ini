[pytest]
testpaths = "tests" 
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning 
markers =
    gpu: any tests related to gpu usage
    gym_install: just show the user that their gymnasium gym_install is correct
    exp_replay: test the ability of the replay buffer to store memories
